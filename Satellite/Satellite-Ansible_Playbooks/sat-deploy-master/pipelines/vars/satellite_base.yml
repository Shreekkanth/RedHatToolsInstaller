server_box:
  box: "{{ pipeline_os }}"
  memory: 8192
capsule_box:
  box: "{{ pipeline_os }}"
  memory: 3072
forklift_boxes:
  "{{ {satdeploy_server_name: server_box, satdeploy_capsule_name: capsule_box} }}"
bats_tests_additional:
  - fb-proxy.bats
