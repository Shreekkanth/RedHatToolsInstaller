---
dependencies:
  - role: hammer_credentials
  - role: foreman_provisioning_infrastructure
    foreman_provisioning_hammer: "{{ satellite_seeder_hammer }}"
    foreman_provisioning_proxy_name: "{{ satellite_seeder_proxy_name }}"
    foreman_provisioning_organization: "{{ satellite_seeder_organization }}"
    foreman_provisioning_location: "{{ satellite_seeder_location }}"
    foreman_provisioning_use_taxonomies: True
    foreman_provisioning_domain: "{{ satellite_seeder_domain }}"
    foreman_provisioning_network: "{{ satellite_seeder_network }}"
    foreman_provisioning_dhcp_start: "{{ satellite_seeder_dhcp_start }}"
    foreman_provisioning_dhcp_end: "{{ satellite_seeder_dhcp_end }}"
    foreman_provisioning_ip_address: "{{ satellite_seeder_ip_address }}"
