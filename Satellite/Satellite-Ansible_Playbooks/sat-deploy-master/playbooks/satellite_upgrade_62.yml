---
- hosts: all
  become: true
  vars:
    activation_key: "Satellite QA RHEL{{ ansible_distribution_major_version }}"
  roles:
    - dogfood_register
    - update_os_packages

- hosts: all
  become: true
  tasks:
    - name: 'Run installer'
      shell: 'satellite-installer --upgrade'
