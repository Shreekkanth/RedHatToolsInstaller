---
- hosts: all
  become: true
  vars:
    activation_key: "Satellite 6.1 Library RHEL{{ ansible_distribution_major_version }}"
  roles:
    - register
    - update_os_packages

- hosts: all
  become: true
  tasks:
    - name: 'Run installer'
      shell: 'katello-installer --upgrade'
