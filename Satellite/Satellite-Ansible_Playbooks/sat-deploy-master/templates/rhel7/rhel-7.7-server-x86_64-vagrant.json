{
    "variables": {
        "user": "vagrant",
        "password": "vagrant",
        "disk_size": "100000"
    },

    "builders":
    [
        {
            "name": "rhel-7.7-x86_64-server-vagrant",

            "type": "qemu",
            "format": "qcow2",
            "accelerator": "kvm",
            "headless": true,
            "disk_size": "{{ user `disk_size`}}",
            "disk_discard": "unmap",
            "disk_interface": "virtio-scsi",

            "iso_url": "http://download.devel.redhat.com/released/RHEL-7/7.7/Server/x86_64/iso/RHEL-7.7-20190723.1-Server-x86_64-boot.iso",
            "iso_checksum": "917e3f3779ce2afac7ff32d697dbe450",
            "iso_checksum_type": "md5",

            "memory": 2048,

            "http_directory": "http",

            "ssh_username": "{{user `user`}}",
            "ssh_password": "{{user `password`}}",
            "ssh_wait_timeout": "20m",
            "shutdown_command": "echo '{{user `password`}}'|sudo -S shutdown -P now",

            "boot_wait": "2s",
            "boot_command": [
                "<tab> linux text biosdevname=0 net.ifnames=0 ip=dhcp ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/ks.cfg<enter><wait>"
            ]
        }
    ],

    "provisioners": [
        {
            "type": "shell",
            "execute_command": "echo '{{user `password`}}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
            "scripts": [
                "scripts/vagrant.sh"
            ]
        },
        {
            "type": "ansible",
            "extra_arguments": [ "--extra-vars", "activation_key={{user `activation_key`}}" ],
            "playbook_file": "../../playbooks/register.yml",
            "ansible_env_vars": [ "ANSIBLE_CONFIG=../ansible.cfg" ]
        },
        {
            "type": "shell",
            "execute_command": "echo '{{user `password`}}' | {{.Vars}} sudo -E -S bash '{{.Path}}'",
            "scripts": [
                "scripts/cleanup.sh"
            ]
        }
    ],

    "post-processors": [
        {
            "keep_input_artifact": false,
            "output": "{{user `build_dir`}}rhel-7.7_x86_64-server.box",
            "type": "vagrant"
        }
    ]
}
