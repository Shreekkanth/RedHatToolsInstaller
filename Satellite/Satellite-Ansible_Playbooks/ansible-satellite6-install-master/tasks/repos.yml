---
#Enabling the repos
- name: rhn | enabling repos for Satellite install 
  command: /usr/bin/subscription-manager repos --disable "*" --enable rhel-7-server-satellite-6.2-rpms --enable rhel-7-server-rpms --enable rhel-server-rhscl-7-rpms
  tags:
    - rhn

#Enabling the repos
- name: rhn | enabling repos for Satellite install 
  command: /usr/bin/subscription-manager repos --enable rhel-7-server-satellite-6.2-rpms 
  tags:
    - rhn

#Enabling the repos
- name: rhn | enabling repos for Satellite install 
  command: /usr/bin/subscription-manager repos  --enable rhel-7-server-rpms 
  tags:
    - rhn

#Enabling the repos
- name: rhn | enabling repos for Satellite install 
  command: /usr/bin/subscription-manager repos --enable rhel-server-rhscl-7-rpms
  tags:
    - rhn
