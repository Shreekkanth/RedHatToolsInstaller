// Endpoints & Zones
object Endpoint "icinga2.srv.cdn.dynetics.com" {
}

object Zone "icinga2.srv.cdn.dynetics.com" {
     endpoints = [ "icinga2.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "icinga2.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.20.254"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ansible.srv.cdn.dynetics.com" {
}

object Zone "ansible.srv.cdn.dynetics.com" {
     endpoints = [ "ansible.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ansible.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.103"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "bastion.srv.cdn.dynetics.com" {
}

object Zone "bastion.srv.cdn.dynetics.com" {
     endpoints = [ "ansible.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "bastion.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.123"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "bastion.srv.cdn.dynetics.com" {
}

object Zone "bastion.srv.cdn.dynetics.com" {
     endpoints = [ "ansible.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "bastion.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.123"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "devvm1.srv.cdn.dynetics.com" {
}

object Zone "devvm1.srv.cdn.dynetics.com" {
     endpoints = [ "devvm1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "devvm1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.151"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "gitlab.srv.cdn.dynetics.com" {
}

object Zone "gitlab.srv.cdn.dynetics.com" {
     endpoints = [ "gitlab.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "gitlab.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.102"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "idm.srv.cdn.dynetics.com" {
}

object Zone "idm.srv.cdn.dynetics.com" {
     endpoints = [ "idm.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "idm.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.100"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "jenkins.srv.cdn.dynetics.com" {
}

object Zone "jenkins.srv.cdn.dynetics.com" {
     endpoints = [ "jenkins.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "jenkins.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.107"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "manager.srv.cdn.dynetics.com" {
}

object Zone "manager.srv.cdn.dynetics.com" {
     endpoints = [ "manager.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "manager.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.211"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpapp1.srv.cdn.dynetics.com" {
}

object Zone "ocpapp1.srv.cdn.dynetics.com" {
     endpoints = [ "ocpapp1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpapp1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.227"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpapp2.srv.cdn.dynetics.com" {
}

object Zone "ocpapp2.srv.cdn.dynetics.com" {
     endpoints = [ "ocpapp2.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpapp2.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.228"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpapp3.srv.cdn.dynetics.com" {
}

object Zone "ocpapp3.srv.cdn.dynetics.com" {
     endpoints = [ "ocpapp3.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpapp3.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.229"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpinfra1.srv.cdn.dynetics.com" {
}

object Zone "ocpinfra1.srv.cdn.dynetics.com" {
     endpoints = [ "ocpinfra1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpinfra1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.224"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpinfra2.srv.cdn.dynetics.com" {
}

object Zone "ocpinfra2.srv.cdn.dynetics.com" {
     endpoints = [ "ocpinfra2.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpinfra2.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.225"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpinfra3.srv.cdn.dynetics.com" {
}

object Zone "ocpinfra3.srv.cdn.dynetics.com" {
     endpoints = [ "ocpinfra3.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpinfra3.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.226"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpmaster1.srv.cdn.dynetics.com" {
}

object Zone "ocpmaster1.srv.cdn.dynetics.com" {
     endpoints = [ "ocpmaster1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpmaster1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.220"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpmaster2.srv.cdn.dynetics.com" {
}

object Zone "ocpmaster2.srv.cdn.dynetics.com" {
     endpoints = [ "ocpmaster2.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpmaster2.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.221"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "ocpmaster3.srv.cdn.dynetics.com" {
}

object Zone "ocpmaster3.srv.cdn.dynetics.com" {
     endpoints = [ "ocpmaster3.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "ocpmaster3.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.222"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "replica01.srv.cdn.dynetics.com" {
}

object Zone "replica01.srv.cdn.dynetics.com" {
     endpoints = [ "replica01.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "replica01.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.200"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "replica02.srv.cdn.dynetics.com" {
}

object Zone "replica02.srv.cdn.dynetics.com" {
     endpoints = [ "replica02.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "replica02.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.20.100"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "rhelrt1.srv.cdn.dynetics.com" {
}

object Zone "rhelrt1.srv.cdn.dynetics.com" {
     endpoints = [ "rhelrt1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "rhelrt1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.14"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "rhsat.srv.cdn.dynetics.com" {
}

object Zone "rhsat.srv.cdn.dynetics.com" {
     endpoints = [ "rhsat.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "rhsat.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.105"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "rhvh1.srv.cdn.dynetics.com" {
}

object Zone "rhvh1.srv.cdn.dynetics.com" {
     endpoints = [ "rhvh1.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "rhvh1.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.11"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "rhvh2.srv.cdn.dynetics.com" {
}

object Zone "rhvh2.srv.cdn.dynetics.com" {
     endpoints = [ "rhvh2.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "rhvh2.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.12"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "rhvh3.srv.cdn.dynetics.com" {
}

object Zone "rhvh3.srv.cdn.dynetics.com" {
     endpoints = [ "rhvh3.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "rhvh3.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.13"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}

// Endpoints & Zones
object Endpoint "sonarqube.srv.cdn.dynetics.com" {
}

object Zone "sonarqube.srv.cdn.dynetics.com" {
     endpoints = [ "sonarqube.srv.cdn.dynetics.com" ]
     parent = "master"
}

// Host Objects
object Host "sonarqube.srv.cdn.dynetics.com" {
    check_command = "hostalive"
    address = "192.168.60.109"
    vars.client_endpoint = name //follows the convention that host name == endpoint name
}